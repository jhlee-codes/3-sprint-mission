# 기본 활성 프로파일
spring:
  profiles:
    active: dev

discodeit:
  storage:
    type: local
    local:
      root-path: .discodeit/storage

# 로깅 레벨
logging:
  level:
    root: INFO # 기본 레벨
    # SQL 로그 출력
    org.hibernate.sql: DEBUG
    org.hibernate.jdbc.bind: TRACE

